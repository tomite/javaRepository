buildscript {
  // 外部リポジトリとしてMavenリポジトリを指定
  repositories {
    mavenCentral()
  }
}
defaultTasks 'clean', 'run'

project("sample:" + properties.sampleDir) {
  apply plugin: 'application'
  mainClassName = 'Hello'
}
